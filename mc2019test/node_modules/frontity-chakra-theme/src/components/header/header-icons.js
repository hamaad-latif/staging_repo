import React from "react";
import { Icon,Stack,SimpleGrid, Box, PseudoBox, Button, Text } from "@chakra-ui/core";
import {
    IoLogoTwitter,
    IoLogoPinterest,
    IoLogoFacebook,
    IoLogoInstagram
  } from "react-icons/io";
import { SiteMenu } from "./navigation";

import {FaWhatsapp, FaPhone} from "react-icons/fa";
  

const menu = [
    ["phone", "+91-xxxxxx"],
    // []
]

export const HeaderMenuIcons = ({ menu}) => (
    <SiteMenu spacing="40px" ml="24px" position={{ sm: "relative" }}>
      {menu.map(([name, link]) => {
        const SocialIcon = icons[name];
        return (
          <HeaderMenuItem key={name} label={name} link={link} icon={SocialIcon} />
        );
      })}
      <Button variantColor="teal" size="lg">
        Button
      </Button>
    </SiteMenu>
  );
  


const HeaderMenuItem = ({ icon, link, ...props }) => (
    <PseudoBox
      color="accent.400"
      transition="all 0.3s"
      _hover={{ color: "white" }}
    //   as="li"
    //   listStyleType="none"
      margin="0"
      {...props}
      
    >
      <Box >
        <Box as={icon} size="24px" />
        <Box>{link}</Box>
      </Box>
    </PseudoBox>
  );
  
  const icons = {
    twitter: IoLogoTwitter,
    pinterest: IoLogoPinterest,
    facebook: IoLogoFacebook,
    instagram: IoLogoInstagram,
    whatsapp: FaWhatsapp,
    phone: FaPhone
  };
  

const SiteMenuIcons = () => {
    return(
    <>  
      <Box as="nav" width="100%" display={{ base: "none", lg: "block" }}>

      <Stack
            ml="50px"
            // spacing="50px"
            as="ul"
            listStyleType="none"
            align="center"
            direction="row"
            color="white"
        >

        <PseudoBox
            color="accent.400"
            transition="all 0.3s"
            _hover={{ color: "white" }}
            as="li"
            listStyleType="none"
            margin="0"
        >
            {/* Default size is 1em => 16px */}
            <SimpleGrid columns={{base:2, md:2}}>
                    {/* <Icon name={FaWhatsapp} /> */}
                {/* </Box> */}
                <Box px="5px">
                    <Icon name="phone" />
                </Box>
                <Box>
                    <Text>+9191919191</Text>
                </Box>
            </SimpleGrid>
        </PseudoBox>
        <PseudoBox
            color="accent.400"
            transition="all 0.3s"
            _hover={{ color: "white" }}
            as="li"
            listStyleType="none"
            margin="0"
        >
            {/* Default size is 1em => 16px */}
            <SimpleGrid columns={{base:2, md:2}}>
                <Box ml="10px" as={icons['whatsapp']} size="24px"/>
                <Box>
                    <Text>Contact&Support</Text>
                </Box>
            </SimpleGrid>
        </PseudoBox>

        <PseudoBox
            color="accent.400"
            transition="all 0.3s"
            _hover={{ color: "white" }}
            as="li"
            listStyleType="none"
            margin="0"
            p={6}
        >
            {/* Default size is 1em => 16px */}
            <SimpleGrid columns={{base:1, md:1}}>
            <Button
                size="md"
                height="48px"
                width="200px"
                border="2px"
                borderColor="green.500"
            > Test
            </Button>
            </SimpleGrid>
        </PseudoBox>
        </Stack>
        </Box>
    </>



);}

export default SiteMenuIcons;